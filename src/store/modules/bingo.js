const state = () => ({});

const getters = {};

const actions = {
  increment() {
    console.log("action");
  }
};

const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
